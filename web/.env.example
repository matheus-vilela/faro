# Ambiente local (opcional): copie para `.env` — gitignored, para segredos locais (ex. ZAPI_CLIENT_TOKEN).
# O Vite já carrega automaticamente:
#   - `.env.development` em `yarn dev`
#   - `.env.production` em `yarn build`
#
# Para testar prod localmente: `yarn env prod` (alterna blocos no `.env` local) e reinicie `yarn dev`.

# --- DEV ---
VITE_SUPABASE_URL=https://your-dev-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-dev-anon-key
VITE_FARO_ENV=dev

# --- PRODUÇÃO ---
# VITE_SUPABASE_URL=https://your-prod-project.supabase.co
# VITE_SUPABASE_ANON_KEY=your-prod-anon-key
# VITE_FARO_ENV=production

# Opcional: sobrescreve o número padrão exibido em Configurações → WhatsApp (padrão: +55 11 917589292)
# VITE_FARO_WHATSAPP_AGENT_DISPLAY="+55 11 917589292"
